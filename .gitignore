# Personal Claude Code permission allowlist — never share.
.claude/settings.local.json
.gstack/
node_modules/

# Local tooling owned by another agent (Google Ads reporting). Netlify publishes the repo
# root, so anything committed here becomes a public URL on joetay.com. Never commit.
tools/
