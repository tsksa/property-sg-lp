{
  "name": "property-sg-lp",
  "version": "1.0.0",
  "private": true,
  "description": "joetay.com \u2014 static site + Netlify Functions (lead capture)",
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "scripts": {
    "check": "node scripts/check-consistency.mjs"
  }
}